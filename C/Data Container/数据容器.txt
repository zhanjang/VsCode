1.结构体 struct 结构体名
{
	数据类型 变量名;
} 实例名称;
2.共用体 union 共用体名 共用体一次只能保存一个成员变量的值,修改某个值会改所有的
{
	数据类型 变量名;
} 实例名称;
结构体和共用体的区别 结构体中的每个变量独自占用内存空间 共用体的所有变量共用一个空间大小为共用体中最大的那一个
3.枚举 enum 枚举名  枚举常用来批量定义整型常量
{
    变量名,
    变量名
}实例名称;